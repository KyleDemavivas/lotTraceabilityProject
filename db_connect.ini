<?php
$dsn = "sqlsrv:Server=localhost;Database=prod_traceability";
$username = "sa";
$password = "Kepi-123";

try {
    $conn = new PDO($dsn, $username, $password);
    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch (PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}

$mysql_host = "ipaddresshere";
$mysql_uname = "a";
$mysql_pword = 'Kepi-123';
$mysql_db = "databasenamehere";

$mysql_conn = new mysqli($mysql_host, $mysql_uname, mysql_pword, $mysql_db);

if($mysql_conn->connect_error){
    exit("MySQL Connection failed: ".$mysql_conn->connect_error);
}
?>